# Rust build output (kept out of source control)
/src-tauri/target/
/src-tauri/permissions/autogenerated/

# JavaScript dependencies and local tooling
/node_modules/
/.npm/

# IDE and editor files
/.idea/
/.vscode/
*.code-workspace

# Local environment and machine-specific files
.env
.env.*
*.local
*.log

# Windows/macOS metadata
Thumbs.db
.DS_Store

# Installers are build products — binaries ship via GitHub Releases.
/installers/

# Freebuff agent workspace (local tooling, never committed)
/.freebuff/

# Known-good release snapshots (local rollback reference, never committed)
/backup/

# Deprecated/original assets backup (local reference only, never committed)
/docs/assets/bad_ori/
